# CMS 测试部署预检与迁移演练

日期：2026-09-14。目标仅为已指定的测试服务器 `124.220.2.69`。

## 当前状态

- 测试 ERP 正常运行，程序提交为 `2a120072`，数据库为 V049。
- 运行中的测试库尚无 TRIPEER 租户、`website_page` 和 `website_page_revision`。
- ERP 本地 main 已包含 CMS 功能，并补齐生成的数据库兼容基线，当前提交 `7fa8a4ac`。
- 50 个迁移文件目录检查通过；现有数据库迁移与安全工作流测试 12 项通过。
- 远端 ERP main 核对时仍为 `87eb191f`，本地发布缓存中没有包含 CMS 的发布包。
- 未推送 GitHub，未更新正在运行的 ERP，未修改生产服务器。

## MySQL 隔离副本演练结果

对运行中的测试库进行一致性备份，将备份恢复到专用副本后执行 V050。副本专用数据库账号无法访问原测试库，演练结束后已删除该临时账号。

- 备份大小：214,286,979 字节，仅保存在测试服务器受限目录，没有下载业务数据或密码到官网项目。
- 备份 SHA-256：`a3a13184ba6fd04ec90bfffc817d2d6160b99348ec1630eb592bab072bf3ad12`。
- V050 SHA-256：`bffb5f1accd86072b3242d440b3a09bc33cd62898c3fbbbf28554667f30c0e1c`。
- SQL 执行成功，创建独立 TRIPEER 配置与品牌运营角色。
- 原有 218 张未涉及的表数据摘要一致；另外 6 张新增配置相关表中的原有记录摘要一致。
- 品牌运营角色仅获得 `seo:page:query/update/publish/preview` 和 `seo:site-config:query`。
- 未创建登录账号或密码；首页草稿和发布版本表初始为空。
- 在专用副本上执行备份恢复，全部 224 张原有表的数据摘要与演练前一致，恢复至 V049。
- 运行中的测试库始终保持 V049。

成功演练回执：测试服务器 `/opt/oakved-deploy/test/rehearsals/cms-v050-20260914-092123-4c34b62b6ad5/receipt.json`。
本地日志：`work/codex-logs/cms-v050-rehearsal-20260914-092118.log`。
数据库测试日志：`work/codex-logs/cms-database-readiness-20260914.log`。

首次数据比对因二进制字段解码失败中止，未执行到 V050；修正校验工具后完成上述演练。首次失败日志保留用于排查。

本次验证的是迁移 SQL 与备份恢复。尚未验证新发布镜像内 Flyway 的实际执行和校验和，不应据此宣称发布镜像、真实 Redis 或运营账号联调已经通过。副本里的 tenantId 也不能直接当作运行中测试库的最终 tenantId。

## 后续执行顺序

1. 获得 GitHub 推送授权后，推送 ERP 已完成的 CMS 代码及基线补全。仓库 `AGENTS.md` 要求授权后才推送；官网项目现有未提交的依赖与构建产物不纳入本次 ERP 推送。
2. 等待该提交的完整 CI 和镜像构建成功，取得准确 release-id；不能借用旧发布包或绕过 CI 验证。
3. 使用新镜像在隔离副本验证 Flyway 升级、重复启动及校验和，结合本次恢复证据登记对应 release-id 的迁移兼容性审核。
4. 按现有测试 CD 流程备份并部署新版本，由 Flyway 更新运行中的测试库，不手工伪造迁移历史。
5. 配置实际站点/预览地址，由管理员创建并分配品牌运营账号，读取最终 tenantId 配置官网代理。
6. 用真实 ERP 验证初始化、保存草稿、整页预览、发布、草稿与正式版隔离，以及家具运营账号的跨租户权限限制。

本次准备未创建运营账号，也未写入账户密码。真实账号创建和登录验证需要管理员的有效登录会话。
