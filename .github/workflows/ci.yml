name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build website
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm ci

      - name: Check deployment scripts without network access
        run: |
          for script in deploy/*.sh; do bash -n "$script"; done
          timeout --kill-after=5s 120s node deploy/test-deploy-cloud.mjs

      - name: Build website
        env:
          VITE_CMS_ENABLED: ${{ vars.VITE_CMS_ENABLED || 'false' }}
          CMS_UPSTREAM: ${{ vars.CMS_UPSTREAM }}
          CMS_TENANT_ID: ${{ vars.CMS_TENANT_ID }}
        run: npm run build

      - name: Test CMS client and deployment readiness checks
        run: timeout --kill-after=5s 60s node --test tests/cms.test.js tests/cms-readiness.test.js tests/cms-preview-traffic.test.js

      - name: Verify build output
        run: |
          test -s dist/index.html
          test -d dist/assets
